<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ServiceGuru" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:windowBackground">@color/white</item>
        <item name="colorAccent">@color/dark_green</item>
        <item name="colorControlActivated">@color/dark_green</item>
        <item name="colorControlHighlight">@color/dark_green</item>
        <item name="colorControlNormal">@color/dark_green</item>

    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/bg_sheet</item>
    </style>
    <style name="Theme.ServiceGuru" parent="Base.Theme.ServiceGuru" />
</resources>